# freezed

## pubspec.yaml

```
dependencies:
  freezed_annotation:

dev_dependencies:
  freezed:
  build_runner:

```

## 実行コマンド

```
flutter pub run build_runner build --delete-conflicting-outputs
```
